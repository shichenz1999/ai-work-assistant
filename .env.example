# Claude (ai_client_api + claude_client_impl)
# Required
ANTHROPIC_API_KEY="sk-ant-xxx"
# Optional
ANTHROPIC_MODEL="claude-haiku-4-5-20251001"

# Gmail (mail_client_api + gmail_client_impl)
# Required
GOOGLE_OAUTH_CLIENT_ID="your-google-oauth-client-id"
GOOGLE_OAUTH_CLIENT_SECRET="your-google-oauth-client-secret"
# Required for non-interactive auth (CI/server)
GOOGLE_OAUTH_REFRESH_TOKEN="your-refresh-token"
# Optional
GOOGLE_OAUTH_SCOPES="https://mail.google.com/"
GMAIL_TOKEN_URI="https://oauth2.googleapis.com/token"

# Orchestrator (service runtime)
# Required
PUBLIC_BASE_URL="http://127.0.0.1:8000"
# Optional
ORCHESTRATOR_AUTH_DB="orchestrator_auth.db"
AUTH_PROVIDERS="google"
MAIL_PROVIDER="google"

# Discord listener
# Required (only if running discord_listener)
DISCORD_BOT_TOKEN="your-discord-bot-token"
